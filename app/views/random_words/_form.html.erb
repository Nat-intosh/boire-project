<%= form_with(model: random_word) do |form| %>
  <% if random_word.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(random_word.errors.count, "error") %> prohibited this random_word from being saved:</h2>

      <ul>
        <% random_word.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :word, style: "display: block" %>
    <%= form.text_field :word %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
